# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9      # required by FastAPI for file (CSV) uploads

# Data / ML  (versions match the environment the model was trained in)
scikit-learn==1.9.0          # must match the version churn_model.pkl was saved with
pandas>=2.2
numpy>=1.26
joblib>=1.3

# Persistence
SQLAlchemy>=2.0,<3.0
